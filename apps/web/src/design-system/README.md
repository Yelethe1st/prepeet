# design-system — ported components

## What this owns

The production port of the prototype design system: tokens, primitives and composite components, with their keyboard behaviour and accessible names.

## What this must never do

It never hardcodes a colour, spacing or radius value outside the tokens, and it never encodes product rules.
