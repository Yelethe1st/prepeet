# lib — cross-cutting browser concerns

## What this owns

Generated API client wrappers, auth session handling, browser observability and the realtime client.

## What this must never do

Nothing here renders. If it renders, it belongs in the design system or a feature.
