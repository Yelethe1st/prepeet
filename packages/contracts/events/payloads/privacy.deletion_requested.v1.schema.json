{
  "$id": "https://contracts.prepeet.com/events/payloads/privacy.deletion_requested.v1.schema.json",
  "title": "privacy.deletion_requested.v1",
  "description": "Deletion of a subject's data was requested. A request rather than a deletion: legal hold, retention obligations and in-flight screening can all defer or narrow it, and a consumer that treated this as done would report compliance that had not happened.",
  "type": "object",
  "additionalProperties": false,
  "x-owner": "privacy",
  "x-consumers": [
    "deletion workflow"
  ],
  "x-since": "1.0",
  "required": [
    "request_id",
    "subject_type",
    "subject_id",
    "scope"
  ],
  "properties": {
    "request_id": {
      "type": "string",
      "description": "The request.",
      "x-since": "1.0",
      "format": "uuid"
    },
    "subject_type": {
      "type": "string",
      "description": "What is being deleted.",
      "x-since": "1.0",
      "enum": [
        "user",
        "candidate",
        "tenant"
      ]
    },
    "subject_id": {
      "type": "string",
      "description": "Which one.",
      "x-since": "1.0",
      "format": "uuid"
    },
    "scope": {
      "type": "string",
      "description": "How far it reaches. Practice data can be deleted while a screening record is retained under an obligation the candidate cannot waive.",
      "x-since": "1.0",
      "enum": [
        "all",
        "practice_only",
        "screening_only"
      ]
    }
  }
}
