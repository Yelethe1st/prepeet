# packages/test-fixtures — shared fixtures

## What this owns

Fixtures shared across languages: sessions, transcripts, evaluations, audio samples and the edge cases the specification names.

## What this must never do

A fixture never contains real candidate data.
