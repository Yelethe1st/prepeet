# services/intelligence — Python intelligence

## What this owns

Typed intelligence: interview composition, runtime policy, extraction, evaluation, articulation, coaching and readiness. It returns schema-valid results to Go.

## What this must never do

It never writes to the product database, never talks to the browser, and never decides a hiring outcome. Go validates everything it returns.
