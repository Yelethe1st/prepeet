# artifacts — versioned content

## What this owns

Personas, interview plans, rules, rubrics, role standards and prompts, versioned and digest-addressed.

## What this must never do

An artifact is never edited in place once published. A change creates a new version, and publication never mutates a session already composed.
