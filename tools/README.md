# tools — developer tooling

## What this owns

Contract checking, seeding, load testing, prompt evaluation and data migration utilities.

## What this must never do

A tool never becomes a runtime dependency of a deployable.
